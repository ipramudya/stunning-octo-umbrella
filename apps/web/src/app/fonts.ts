import { Inter, Manrope } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], variable: '--font-sans' });
export const manropeHeading = Manrope({
  subsets: ['latin'],
  variable: '--font-heading',
});
