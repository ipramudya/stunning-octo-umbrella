// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.2
//   protoc               v3.19.1
// source: dexa/attendance/v1/attendance.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Empty } from "../../../google/protobuf/empty.js";

export const protobufPackage = "dexa.attendance.v1";

export interface AttendanceZone {
  name: string;
  address: string;
  latitude: number;
  longitude: number;
  radiusMeters: number;
  active: boolean;
}

export interface UpdateAttendanceZoneRequest {
  name: string;
  address: string;
  latitude: number;
  longitude: number;
  radiusMeters: number;
  active: boolean;
}

function createBaseAttendanceZone(): AttendanceZone {
  return { name: "", address: "", latitude: 0, longitude: 0, radiusMeters: 0, active: false };
}

export const AttendanceZone: MessageFns<AttendanceZone> = {
  encode(message: AttendanceZone, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.latitude !== 0) {
      writer.uint32(25).double(message.latitude);
    }
    if (message.longitude !== 0) {
      writer.uint32(33).double(message.longitude);
    }
    if (message.radiusMeters !== 0) {
      writer.uint32(41).double(message.radiusMeters);
    }
    if (message.active !== false) {
      writer.uint32(48).bool(message.active);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttendanceZone {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAttendanceZone();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.address = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.latitude = reader.double();
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.longitude = reader.double();
            continue;
          }
          case 5: {
            if (tag !== 41) {
              break;
            }

            message.radiusMeters = reader.double();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.active = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AttendanceZone {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      latitude: isSet(object.latitude) ? globalThis.Number(object.latitude) : 0,
      longitude: isSet(object.longitude) ? globalThis.Number(object.longitude) : 0,
      radiusMeters: isSet(object.radiusMeters)
        ? globalThis.Number(object.radiusMeters)
        : isSet(object.radius_meters)
        ? globalThis.Number(object.radius_meters)
        : 0,
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
    };
  },

  toJSON(message: AttendanceZone): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.latitude !== 0) {
      obj.latitude = message.latitude;
    }
    if (message.longitude !== 0) {
      obj.longitude = message.longitude;
    }
    if (message.radiusMeters !== 0) {
      obj.radiusMeters = message.radiusMeters;
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    return obj;
  },

  create(base?: DeepPartial<AttendanceZone>): AttendanceZone {
    return AttendanceZone.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AttendanceZone>): AttendanceZone {
    const message = createBaseAttendanceZone();
    message.name = object.name ?? "";
    message.address = object.address ?? "";
    message.latitude = object.latitude ?? 0;
    message.longitude = object.longitude ?? 0;
    message.radiusMeters = object.radiusMeters ?? 0;
    message.active = object.active ?? false;
    return message;
  },
};

function createBaseUpdateAttendanceZoneRequest(): UpdateAttendanceZoneRequest {
  return { name: "", address: "", latitude: 0, longitude: 0, radiusMeters: 0, active: false };
}

export const UpdateAttendanceZoneRequest: MessageFns<UpdateAttendanceZoneRequest> = {
  encode(message: UpdateAttendanceZoneRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.latitude !== 0) {
      writer.uint32(25).double(message.latitude);
    }
    if (message.longitude !== 0) {
      writer.uint32(33).double(message.longitude);
    }
    if (message.radiusMeters !== 0) {
      writer.uint32(41).double(message.radiusMeters);
    }
    if (message.active !== false) {
      writer.uint32(48).bool(message.active);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateAttendanceZoneRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUpdateAttendanceZoneRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.address = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.latitude = reader.double();
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.longitude = reader.double();
            continue;
          }
          case 5: {
            if (tag !== 41) {
              break;
            }

            message.radiusMeters = reader.double();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.active = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UpdateAttendanceZoneRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      latitude: isSet(object.latitude) ? globalThis.Number(object.latitude) : 0,
      longitude: isSet(object.longitude) ? globalThis.Number(object.longitude) : 0,
      radiusMeters: isSet(object.radiusMeters)
        ? globalThis.Number(object.radiusMeters)
        : isSet(object.radius_meters)
        ? globalThis.Number(object.radius_meters)
        : 0,
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
    };
  },

  toJSON(message: UpdateAttendanceZoneRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.latitude !== 0) {
      obj.latitude = message.latitude;
    }
    if (message.longitude !== 0) {
      obj.longitude = message.longitude;
    }
    if (message.radiusMeters !== 0) {
      obj.radiusMeters = message.radiusMeters;
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateAttendanceZoneRequest>): UpdateAttendanceZoneRequest {
    return UpdateAttendanceZoneRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateAttendanceZoneRequest>): UpdateAttendanceZoneRequest {
    const message = createBaseUpdateAttendanceZoneRequest();
    message.name = object.name ?? "";
    message.address = object.address ?? "";
    message.latitude = object.latitude ?? 0;
    message.longitude = object.longitude ?? 0;
    message.radiusMeters = object.radiusMeters ?? 0;
    message.active = object.active ?? false;
    return message;
  },
};

export type AttendanceServiceService = typeof AttendanceServiceService;
export const AttendanceServiceService = {
  getAttendanceZone: {
    path: "/dexa.attendance.v1.AttendanceService/GetAttendanceZone" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    requestDeserialize: (value: Buffer): Empty => Empty.decode(value),
    responseSerialize: (value: AttendanceZone): Buffer => Buffer.from(AttendanceZone.encode(value).finish()),
    responseDeserialize: (value: Buffer): AttendanceZone => AttendanceZone.decode(value),
  },
  updateAttendanceZone: {
    path: "/dexa.attendance.v1.AttendanceService/UpdateAttendanceZone" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateAttendanceZoneRequest): Buffer =>
      Buffer.from(UpdateAttendanceZoneRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateAttendanceZoneRequest => UpdateAttendanceZoneRequest.decode(value),
    responseSerialize: (value: AttendanceZone): Buffer => Buffer.from(AttendanceZone.encode(value).finish()),
    responseDeserialize: (value: Buffer): AttendanceZone => AttendanceZone.decode(value),
  },
} as const;

export interface AttendanceServiceServer extends UntypedServiceImplementation {
  getAttendanceZone: handleUnaryCall<Empty, AttendanceZone>;
  updateAttendanceZone: handleUnaryCall<UpdateAttendanceZoneRequest, AttendanceZone>;
}

export interface AttendanceServiceClient extends Client {
  getAttendanceZone(
    request: Empty,
    callback: (error: ServiceError | null, response: AttendanceZone) => void,
  ): ClientUnaryCall;
  getAttendanceZone(
    request: Empty,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AttendanceZone) => void,
  ): ClientUnaryCall;
  getAttendanceZone(
    request: Empty,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AttendanceZone) => void,
  ): ClientUnaryCall;
  updateAttendanceZone(
    request: UpdateAttendanceZoneRequest,
    callback: (error: ServiceError | null, response: AttendanceZone) => void,
  ): ClientUnaryCall;
  updateAttendanceZone(
    request: UpdateAttendanceZoneRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AttendanceZone) => void,
  ): ClientUnaryCall;
  updateAttendanceZone(
    request: UpdateAttendanceZoneRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AttendanceZone) => void,
  ): ClientUnaryCall;
}

export const AttendanceServiceClient = makeGenericClientConstructor(
  AttendanceServiceService,
  "dexa.attendance.v1.AttendanceService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): AttendanceServiceClient;
  service: typeof AttendanceServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
